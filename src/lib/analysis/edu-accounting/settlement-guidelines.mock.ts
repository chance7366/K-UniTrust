/**
 * 목업은 프로덕션 지침과 같은 본문을 쓴다. 버전만 목업으로 표시한다.
 */
export {
  EDU_SETTLEMENT_CHAPTER_DETAIL,
  EDU_SETTLEMENT_FILTERS,
  EDU_SETTLEMENT_IDENTITY,
  EDU_SETTLEMENT_OUTLINE,
  buildEduSettlementGuidelines as buildEduSettlementGuidelinesMock,
} from "./settlement-guidelines";

export const EDU_SETTLEMENT_GUIDELINES_VERSION = "1.0.0-mock";
