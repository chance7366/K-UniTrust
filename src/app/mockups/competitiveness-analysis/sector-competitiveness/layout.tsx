import { AppShell } from "@/components/layout/AppShell";

export default function SectorCompetitivenessMockLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppShell>{children}</AppShell>;
}
