import { AppShell } from "@/components/layout/AppShell";

export default function CompetitivenessRiskUniversitiesMockLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppShell>{children}</AppShell>;
}
