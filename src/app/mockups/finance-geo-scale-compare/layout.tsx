import { AppShell } from "@/components/layout/AppShell";

export default function FinanceGeoScaleCompareMockLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppShell>{children}</AppShell>;
}
