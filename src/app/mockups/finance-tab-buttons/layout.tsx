import { AppShell } from "@/components/layout/AppShell";

export default function FinanceTabButtonsMockLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppShell>{children}</AppShell>;
}
