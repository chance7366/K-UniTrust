# env / secrets
.env
.env.local
.env.*.local

# dependencies
node_modules/

# next.js
.next/
out/

# generated map assets (npm postinstall copies from cesium)
public/cesium/*
!public/cesium/.gitkeep

# misc
.DS_Store
Thumbs.db
*.pem
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local tooling
.playwright-mcp/
.cursor/
.vscode/

# 업로드 원본 스냅샷(용량·중복). 운영 CSV는 data/csv 유지
data/01_raw/api/

# 실행 결과 JSON — 재정추계는 로컬 전용, 경쟁력 에디션은 배포 시 포함
data/json/financial-projection/**/*.json

# vercel
.vercel
